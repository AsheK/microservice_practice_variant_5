# Production-Ready Комментарий: Управление зависимостями
# В реальных проектах версии закрепляются более строго (например, с помощью poetry или pip-tools)
# для обеспечения детерминированных сборок.
flask>=3.0
grpcio>=1.40.0
protobuf>=3.19.0  # Явно добавляем зависимость protobuf
python-dotenv  # Для удобной работы с переменными окружения при локальной разработке
pytest         # Для тестирования